import { RoomBuilder } from "../../builder/RoomBuilder";

describe("ResourceJobPlanner", () => {
    let _roomBuilder: RoomBuilder;

    beforeEach(() => {
    });

    describe("in a room without any resources", () => {

    });
});
